import { RecebedoresPipe } from './recebedores.pipe';

describe('RecebedoresPipe', () => {
  it('create an instance', () => {
    const pipe = new RecebedoresPipe();
    expect(pipe).toBeTruthy();
  });
});
